import { getAllPages, getCurrentLevelPages, getPagesUnderRoute } from 'nextra/context'
import { BlogCardList } from '@/components/ui/blog-card-list'
import { TagList } from '@/components/ui/tag-list'
import { transformMeta, getTags } from '@/lib/get-post-from-files'
import { Bleed } from 'nextra-theme-docs'

<Bleed>
  <TagList tags={getTags(getPagesUnderRoute('/posts'))} withCount asLink className='mb-20 mt-10' />
  <BlogCardList articles={transformMeta(getPagesUnderRoute('/posts'))} />
</Bleed>
